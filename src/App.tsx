export default function App() {
  return (
    <main className="flex min-h-svh flex-col items-center justify-center gap-4 p-8">
      <h1 className="text-2xl font-medium text-fg">WMDS</h1>
      <p className="max-w-md text-center text-muted">
        Run <code className="rounded-md bg-surface px-1.5 py-0.5 text-sm">npm run storybook</code>{" "}
        for the Foundation catalog — components rebuild from there.
      </p>
    </main>
  );
}
