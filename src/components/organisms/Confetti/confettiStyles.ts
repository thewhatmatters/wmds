/** Fixed, click-through overlay. `--z-confetti` sits above Dialog / Sheet / Panel and Toast. */
export const confettiLayerClasses =
  "pointer-events-none fixed inset-0 z-[var(--z-confetti)] overflow-visible";
